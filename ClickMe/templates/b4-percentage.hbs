<div class="form-group{{#if required}} has-feedback{{/if}}">
    <label for="{{name}}" class="col-sm-3 control-label">{{caption}}</label>
    <div class="col-sm-9">
        <div class="input-group">
            <input type="text" class="form-control az-percentage" id="{{name}}" name="{{name}}" value="{{value}}" placeholder="{{placeholder}}" />
            <span class="input-group-addon">%</span>
            {{#if required}}
            <i class="fa fa-asterisk form-control-feedback"></i>
            {{/if}}
        </div>
    </div>
</div>